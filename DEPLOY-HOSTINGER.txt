═══════════════════════════════════════════════════════════════════════════════
  🚀 GUIA COMPLETO: DEPLOY DO SISTEMA DE ESTOQUE NA HOSTINGER
═══════════════════════════════════════════════════════════════════════════════

📋 PRÉ-REQUISITOS
═══════════════════════════════════════════════════════════════════════════════
✅ Conta na Hostinger (Plano Business ou superior)
✅ Domínio configurado (ex: seurestaurante.com)
✅ Acesso ao painel hPanel da Hostinger
✅ Cliente FTP (FileZilla) ou Git instalado


═══════════════════════════════════════════════════════════════════════════════
PASSO 1: PREPARAR O PROJETO LOCALMENTE
═══════════════════════════════════════════════════════════════════════════════

1.1 - BUILD DO FRONTEND
------------------------
No terminal, dentro da pasta /frontend:

cd frontend
npm install
npm run build

✅ Isso cria a pasta /frontend/build com os arquivos otimizados


1.2 - PREPARAR BACKEND
----------------------
No terminal, dentro da pasta /backend:

cd backend
npm install --production

✅ Instala apenas dependências de produção


1.3 - CRIAR ARQUIVO .env DE PRODUÇÃO
-------------------------------------
Criar arquivo: backend/.env.production

Conteúdo:
---------
PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_NAME=estoque_restaurante
DB_USER=seu_usuario_hostinger
DB_PASSWORD=sua_senha_hostinger
NODE_ENV=production


═══════════════════════════════════════════════════════════════════════════════
PASSO 2: CONFIGURAR BANCO DE DADOS NA HOSTINGER
═══════════════════════════════════════════════════════════════════════════════

2.1 - ACESSAR PAINEL hPanel
----------------------------
1. Login em: https://hpanel.hostinger.com
2. Ir em: "Banco de Dados" → "PostgreSQL"


2.2 - CRIAR BANCO DE DADOS
---------------------------
1. Clicar em "Criar Banco de Dados"
2. Nome: estoque_restaurante
3. Usuário: criar novo usuário
4. Senha: gerar senha forte
5. Anotar as credenciais:
   - Host: localhost (ou IP fornecido)
   - Porta: 5432
   - Nome do banco: estoque_restaurante
   - Usuário: [anotar]
   - Senha: [anotar]


2.3 - IMPORTAR ESTRUTURA DO BANCO
----------------------------------
1. No painel, clicar em "phpPgAdmin" ou "Adminer"
2. Fazer login com as credenciais criadas
3. Selecionar o banco "estoque_restaurante"
4. Ir em "SQL" ou "Importar"
5. Copiar e executar o conteúdo de: /database/init.sql
6. (Opcional) Executar: /database/seed-produtos.sql para dados de teste


═══════════════════════════════════════════════════════════════════════════════
PASSO 3: CONFIGURAR NODE.JS NA HOSTINGER
═══════════════════════════════════════════════════════════════════════════════

3.1 - ATIVAR NODE.JS
--------------------
1. No hPanel, ir em: "Avançado" → "Node.js"
2. Clicar em "Criar Aplicação"
3. Configurações:
   - Versão Node.js: 18.x ou superior
   - Modo: Produção
   - Diretório da aplicação: /backend
   - Arquivo de inicialização: server.js
   - Porta: 3001


3.2 - VARIÁVEIS DE AMBIENTE
----------------------------
No painel Node.js, adicionar variáveis:

PORT=3001
DB_HOST=localhost
DB_PORT=5432
DB_NAME=estoque_restaurante
DB_USER=[seu_usuario]
DB_PASSWORD=[sua_senha]
NODE_ENV=production


═══════════════════════════════════════════════════════════════════════════════
PASSO 4: UPLOAD DOS ARQUIVOS
═══════════════════════════════════════════════════════════════════════════════

OPÇÃO A: VIA FTP (FileZilla)
-----------------------------
1. Abrir FileZilla
2. Conectar com credenciais FTP da Hostinger:
   - Host: ftp.seudominio.com
   - Usuário: [fornecido pela Hostinger]
   - Senha: [fornecido pela Hostinger]
   - Porta: 21

3. Estrutura de pastas no servidor:
   /public_html/               (raiz do site)
   ├── index.html              (do build do React)
   ├── static/                 (do build do React)
   ├── asset-manifest.json     (do build do React)
   └── ...outros arquivos do build

   /backend/                   (fora do public_html)
   ├── controllers/
   ├── models/
   ├── routes/
   ├── config/
   ├── server.js
   ├── package.json
   └── node_modules/

4. Upload:
   - Enviar conteúdo de /frontend/build/* para /public_html/
   - Enviar pasta /backend completa para /backend/


OPÇÃO B: VIA GIT (Recomendado)
-------------------------------
1. No hPanel, ir em "Git" → "Criar Repositório"
2. Conectar com GitHub/GitLab
3. Fazer push do projeto:

git init
git add .
git commit -m "Deploy inicial"
git remote add origin [url-do-repositorio]
git push -u origin main

4. Configurar deploy automático no painel


═══════════════════════════════════════════════════════════════════════════════
PASSO 5: CONFIGURAR FRONTEND PARA APONTAR PARA API
═══════════════════════════════════════════════════════════════════════════════

5.1 - ATUALIZAR URL DA API
---------------------------
Editar: frontend/src/services/api.js

ANTES (desenvolvimento):
const API_URL = 'http://localhost:3001';

DEPOIS (produção):
const API_URL = 'https://seudominio.com/api';
// OU
const API_URL = process.env.REACT_APP_API_URL || 'https://seudominio.com/api';


5.2 - CRIAR .env NO FRONTEND
-----------------------------
Criar: frontend/.env.production

REACT_APP_API_URL=https://seudominio.com/api


5.3 - REBUILD DO FRONTEND
--------------------------
cd frontend
npm run build

Fazer upload novamente dos arquivos do /build para /public_html/


═══════════════════════════════════════════════════════════════════════════════
PASSO 6: CONFIGURAR PROXY/REDIRECIONAMENTO
═══════════════════════════════════════════════════════════════════════════════

6.1 - CRIAR .htaccess
---------------------
Criar arquivo: /public_html/.htaccess

Conteúdo:
---------
# Redirecionar API para backend Node.js
RewriteEngine On

# Redirecionar /api/* para Node.js na porta 3001
RewriteRule ^api/(.*)$ http://localhost:3001/$1 [P,L]

# React Router - redirecionar tudo para index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]


═══════════════════════════════════════════════════════════════════════════════
PASSO 7: INICIAR APLICAÇÃO NODE.JS
═══════════════════════════════════════════════════════════════════════════════

7.1 - VIA PAINEL HOSTINGER
---------------------------
1. Ir em "Node.js" no hPanel
2. Selecionar a aplicação criada
3. Clicar em "Iniciar" ou "Restart"


7.2 - VIA SSH (Alternativo)
----------------------------
1. Conectar via SSH:
   ssh usuario@seudominio.com

2. Navegar até a pasta:
   cd backend

3. Instalar dependências:
   npm install --production

4. Iniciar com PM2:
   pm2 start server.js --name "estoque-api"
   pm2 save
   pm2 startup


═══════════════════════════════════════════════════════════════════════════════
PASSO 8: TESTAR A APLICAÇÃO
═══════════════════════════════════════════════════════════════════════════════

8.1 - TESTAR FRONTEND
---------------------
Acessar: https://seudominio.com
✅ Deve carregar a página inicial do React


8.2 - TESTAR API
----------------
Acessar: https://seudominio.com/api/estoque
✅ Deve retornar JSON com produtos


8.3 - TESTAR INTEGRAÇÃO
------------------------
1. Abrir o site: https://seudominio.com
2. Navegar até "Produtos Estoque"
3. Verificar se os produtos carregam
4. Tentar adicionar um produto
5. Verificar se salva no banco


═══════════════════════════════════════════════════════════════════════════════
PASSO 9: CONFIGURAÇÕES DE SEGURANÇA (IMPORTANTE!)
═══════════════════════════════════════════════════════════════════════════════

9.1 - ATIVAR HTTPS/SSL
----------------------
1. No hPanel, ir em "Segurança" → "SSL/TLS"
2. Ativar "Let's Encrypt SSL" (gratuito)
3. Forçar HTTPS (redirecionar HTTP → HTTPS)


9.2 - PROTEGER ARQUIVOS SENSÍVEIS
----------------------------------
Adicionar no .htaccess:

# Bloquear acesso a arquivos sensíveis
<FilesMatch "^\.env">
    Order allow,deny
    Deny from all
</FilesMatch>


9.3 - CONFIGURAR CORS NO BACKEND
---------------------------------
Editar: backend/server.js

const cors = require('cors');
app.use(cors({
  origin: 'https://seudominio.com',
  credentials: true
}));


═══════════════════════════════════════════════════════════════════════════════
PASSO 10: MANUTENÇÃO E MONITORAMENTO
═══════════════════════════════════════════════════════════════════════════════

10.1 - LOGS DO NODE.JS
----------------------
Via painel: Node.js → Ver Logs
Via SSH: pm2 logs estoque-api


10.2 - BACKUP DO BANCO
----------------------
1. No hPanel, ir em "Banco de Dados"
2. Selecionar o banco
3. Clicar em "Exportar" ou "Backup"
4. Agendar backups automáticos


10.3 - ATUALIZAR APLICAÇÃO
---------------------------
1. Fazer alterações localmente
2. Build do frontend: npm run build
3. Upload dos novos arquivos via FTP/Git
4. Reiniciar Node.js no painel


═══════════════════════════════════════════════════════════════════════════════
TROUBLESHOOTING - PROBLEMAS COMUNS
═══════════════════════════════════════════════════════════════════════════════

❌ ERRO: "Cannot connect to database"
--------------------------------------
✅ Verificar credenciais no .env
✅ Confirmar que PostgreSQL está ativo
✅ Testar conexão via phpPgAdmin


❌ ERRO: "404 Not Found" na API
--------------------------------
✅ Verificar .htaccess
✅ Confirmar que Node.js está rodando
✅ Verificar porta 3001 no painel


❌ ERRO: "CORS policy blocked"
-------------------------------
✅ Adicionar CORS no backend
✅ Verificar URL da API no frontend
✅ Confirmar HTTPS está ativo


❌ Frontend carrega, mas não mostra dados
------------------------------------------
✅ Abrir DevTools (F12) → Console
✅ Verificar erros de requisição
✅ Confirmar URL da API está correta
✅ Testar API diretamente no navegador


═══════════════════════════════════════════════════════════════════════════════
CHECKLIST FINAL
═══════════════════════════════════════════════════════════════════════════════

□ Banco de dados PostgreSQL criado e populado
□ Credenciais do banco anotadas
□ Backend enviado para /backend/
□ Frontend (build) enviado para /public_html/
□ Node.js configurado e rodando
□ Variáveis de ambiente configuradas
□ .htaccess criado com proxy
□ SSL/HTTPS ativado
□ CORS configurado
□ Testes realizados (frontend + API + integração)
□ Backup do banco configurado


═══════════════════════════════════════════════════════════════════════════════
CUSTOS ESTIMADOS (HOSTINGER)
═══════════════════════════════════════════════════════════════════════════════

Plano Business:     ~R$ 15-20/mês
- Node.js incluído
- PostgreSQL incluído
- 100GB armazenamento
- SSL gratuito
- Domínio grátis (1º ano)


═══════════════════════════════════════════════════════════════════════════════
SUPORTE
═══════════════════════════════════════════════════════════════════════════════

Hostinger: https://www.hostinger.com.br/suporte
Chat 24/7 em português
Tutoriais: https://support.hostinger.com


═══════════════════════════════════════════════════════════════════════════════
OBSERVAÇÕES FINAIS
═══════════════════════════════════════════════════════════════════════════════

⚠️  NUNCA commitar arquivos .env no Git
⚠️  Sempre usar HTTPS em produção
⚠️  Fazer backups regulares do banco
⚠️  Monitorar logs para erros
⚠️  Manter Node.js e dependências atualizadas

✅ Após seguir todos os passos, seu sistema estará 100% funcional na Hostinger!

═══════════════════════════════════════════════════════════════════════════════
